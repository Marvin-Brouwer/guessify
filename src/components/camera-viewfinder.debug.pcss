.camera-canvas .video-overlay.debug {
  clip-path: unset !important;
  backdrop-filter: unset !important;
  background: none;
  .viewfinder {
    canvas {
      display: unset !important;
	  touch-action: none;
	  pointer-events: none;
	  position: absolute;
	  left: 0;
	  top: 0;
	  margin: -4px 0 0 -4px;

      &#scale {
        opacity: 0.6;
        position: fixed;
        margin: 0;
      }
      &#grayscale {
        opacity: 0.3;
      }
	  &#blur {
		display:none !important;
	  }
	  &#edge {
        opacity: 0.7;
	  }
    }
    &:hover canvas#grayscale,
    &:active canvas#grayscale {
      opacity: 1;
    }
  }
}
